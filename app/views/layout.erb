<!DOCTYPE html>
<head>
  <meta charset="utf-8" />
  <title>ThePrancingPages</title>
  <!-- This is where I need to link any future .css -->
</head>

<body>

  <nav>
  <% if logged_in? %>
    Currently signed in as <strong><%= current_user.username %></strong>, <a href="/users/<%= current_user.id.to_s %>">Edit Profile</a>
     OR <a href="/logout">Log Out</a>
  <% else %>
    <a href="/users/new">Sign Up!</a> OR <a href="/login">Login!</a>
  <% end %>
    | <a href="/characters">View All Characters</a>
  </nav>

  <%= yield %>

</body>
</html>