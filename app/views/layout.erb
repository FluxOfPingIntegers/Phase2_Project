<!DOCTYPE html>
<head>
  <meta charset="utf-8" />
  <title>ThePrancingPages</title>
  <link rel="stylesheet" type="text/css" href="/styles/style.css?version=1"/>
</head>

<body>

  <nav>
  <% if logged_in? %>
    Currently signed in as <strong><%= current_user.username %></strong>, <a href="/user/<%= current_user.id.to_s %>">View Profile</a>
     OR <a href="/logout">Log Out</a>
  <% else %>
    <a href="/user/new">Sign Up!</a> OR <a href="/login">Login!</a>
  <% end %>
    | <a href="/">Home<a> | <a href="/characters">View All Characters</a>
  </nav>

  <div class="border" id="border">

    <div class="pages">
      <div class="text" id="content">
        <%= yield %>
      </div>
    </div>
  </div>

</body>
</html>

