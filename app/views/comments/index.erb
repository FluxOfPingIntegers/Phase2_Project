<h1>users have submitted all of the following comments to <a href="/characters/<%= @character.id %>"><%= @character.name %></a></h1>
  <br>
  <% if !@character.comments.empty? %>
    <ul>
    <% @character.comments.reverse.each do |comment| %>
      <br>
      <li><strong><%= comment.user.username %></strong>: "<%= comment.content %>"</li>
      <% if comment.user_id == session[:user_id] %>
        Click <a href="/characters/<%=comment.character.id%>/comments/<%=comment.id%>/edit">here</a> to edit this comment.<br>
      <% end %>
    <% end %>
    </ul>
  <% else %>
    <h3>This character currently has no comments!<h3><br>
    <p>Would you like to be the first to comment??? Click <a href="/characters/<%=@character.id%>/comments/new">here</a>!!!</p>
  <% end %>